{
  "name": "projectarmageddon",
  "version": "0.1.0",
  "description": "Deterministic 2D artillery tactics prototype for the browser.",
  "type": "module",
  "main": "src/server/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate": "node --input-type=module -e \"await import('./src/shared/index.js'); await import('./src/engine/index.js'); await import('./src/server/index.js'); await import('./src/client/index.js'); console.log('ProjectArmageddon skeleton is valid.');\"",
    "test": "node --test tests/*.test.js --test-reporter=spec",
    "test:unit": "node tests/prng.test.js && node tests/seed.test.js && node tests/loot.test.js",
    "test:e2e": "playwright test",
    "test:all": "npm run test && npm run test:e2e",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "smoke": "node scripts/smoke-match.mjs",
    "perf": "node scripts/perf-profile.mjs",
    "balance": "node scripts/balance-report.mjs",
    "balance:sweep": "node scripts/balance-report.mjs --sweep",
    "replay": "node scripts/replay.mjs",
    "icons": "python3 scripts/create-weapon-icons.py",
    "weapons:build": "node scripts/build-weapon-catalog.mjs"
  },
  "keywords": [
    "artillery",
    "ecs",
    "multiplayer",
    "canvas",
    "webgame"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
